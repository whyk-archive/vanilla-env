{
  "private": true,
  "scripts": {
    "dev": "parcel src/**/*.html --open --no-cache",
    "prebuild": "rimraf dist/",
    "build": "parcel build src/index.html src/**/*.html --public-url ."
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.0",
    "sass": "^1.23.7",
    "typescript": "^3.7.3"
  }
}
